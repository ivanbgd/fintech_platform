//! The program's entry point.
//!
//! It should generally contain only the `main` function,
//! but I decided to keep the provided manual testing function here,
//! too, as it is not an automated integration test.

use fintech_platform::logic::main_loop;

fn main() {
    main_loop();
}
